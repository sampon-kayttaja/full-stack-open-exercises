{
  "name": "blogilista",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test node --test",
    "start": "NODE_ENV=produciton node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd ../bloglist-frontend && npm run build && cp -r dist ../bloglist-backend",
    "start:test": "npm run build:ui && cross-env NODE_ENV=test node index.js",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "eslint": "^10.6.0",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "supertest": "^7.2.2"
  }
}
